[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "eaggl"
version = "0.1.0"
description = "EAGGL factoring workflows split from PIGEAN"
requires-python = ">=3.10"
